services:
  web:
    build: .
    ports:
      - "${DJANGO_BIND_HOST:-0.0.0.0}:${DJANGO_PORT:-8768}:${DJANGO_PORT:-8768}"
    volumes:
      - ./db_data:/data
      - ./media_data:/app/media
    env_file:
      - .env
    restart: unless-stopped
